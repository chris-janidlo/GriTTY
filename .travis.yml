language: python
sudo: false

addons:
  apt:
    packages:
      - lua5.1
      - libzip-dev
      - luarocks

install:
  - luarocks install love-release

script:
  - love-release -M $TRAVIS_BUILD_DIR src
